import React from "react";

const Header = () => {
  return (
    <header
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        width: "40%",
      }}
    >
      <h1>PokéFinder</h1>
    </header>
  );
};

export default Header;
